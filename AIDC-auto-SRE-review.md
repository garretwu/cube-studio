# AIDC Auto SRE Agent 设计评审（修订版）

> 评审目标：在不改变“自由诊断、受控修复”总体架构的前提下，给出**可直接进入开发排期**的缺陷清单与修复建议。  
> 评审结论：方案方向正确，但当前文档存在 2 个 P0（Demo/安全阻塞）、6 个 P1（正确性/稳定性风险）与若干 P2（工程化优化项）。建议先完成 P0+关键 P1，再组织端到端演示。

---

## 1. 总体评价

### 1.1 做得好的部分
- 架构边界清楚：将“LLM 诊断”与“确定性修复执行”分层，符合 SRE 可审计要求。
- 安全机制设计完整：WAL、审批门控、灰度修复、回滚路径都已纳入。
- 数据闭环意识较强：拓扑（Ontology）+ 知识库（RAG）+ 记忆系统（Memory）组成持续学习链路。

### 1.2 当前主要风险
- 文档中的“安全原则”与“实现细节”存在冲突（动态求值、命令拼接）。
- 多处 async 代码路径落到同步 I/O，生产并发下会出现性能与稳定性问题。
- 诊断工具可见性与修复计划生成之间存在信息不对称，容易产出不可执行计划。

---

## 2. 关键问题（按严重度）

## P0（必须先修复）

### P0-1：Demo 关键步骤的工具调用与工具定义不一致
**现象**  
Demo Trace 中调用 `read_system_log(..., command="nvidia-smi ...")`，但工具定义仅支持 `log_path` + `tail`，实现也仅是 `tail -n {tail} {log_path}`。

**影响**  
- Demo 中“定位 GPU 异常进程”步骤不可执行。
- 诊断链路在关键证据采集点中断。

**建议（最小改动）**  
- 新增只读工具：`get_gpu_processes(node)`，专门封装 `nvidia-smi --query-compute-apps`。
- 不建议把 `read_system_log` 扩展成“任意命令执行”入口（会扩大攻击面）。

---

### P0-2：修复验证使用动态表达式求值，存在代码执行风险
**现象**  
`success_condition` 由 LLM 生成并被动态求值（如 `eval` 风格）。

**影响**  
- 一旦被 prompt injection 或模型幻觉利用，可能触发任意代码执行。
- 与“LLM 不直接执行写操作”的核心不变量冲突。

**建议**  
- 将验证条件改为结构化模型：
  - `field`: 指标字段名
  - `operator`: `< | <= | > | >= | ==`
  - `value`: 阈值
- 若必须保留表达式语法：采用 AST 白名单，仅允许比较与布尔运算，禁止函数调用/属性访问/导入。

---

## P1（建议本迭代修复）

### P1-1：Blast Radius 仅按单向边遍历，影响面可能漏算
**现象**  
拓扑关系中存在“子->父”和“服务->实例”等方向，若只看 out_edges 会遗漏反向依赖。

**影响**  
- 初始诊断上下文不完整。
- 根因排序偏差，导致错误修复优先级。

**建议**  
- 按关系类型定义传播方向（上游/下游/双向）。
- 遍历时结合 in_edges + out_edges。
- 增加标准拓扑单测：Node 故障必须命中其 Pod/Service/上层业务对象。

---

### P1-2：Alert 模型混用 dataclass 与 Pydantic，序列化/校验接口不兼容
**现象**  
`@dataclass` 类型被当作 Pydantic `model_validate` 输入，接口语义不一致。

**影响**  
- 运行时校验报错或隐式失败。
- Incident 记录链路不稳定。

**建议**  
- 统一为 Pydantic BaseModel（或统一 dataclass + 明确转换层），不要混用。

---

### P1-3：async 方法内调用同步 sqlite/chroma 接口
**现象**  
知识检索、记忆读写、拓扑持久化在 async 路径中存在同步调用。

**影响**  
- 阻塞事件循环，WebSocket 推送与多会话诊断抖动明显。
- 并发告警时 P95 响应显著上升。

**建议**  
- sqlite -> `aiosqlite`。
- 暂时无法替换的同步 SDK 用 `asyncio.to_thread` 隔离。
- 增加关键 I/O 超时与熔断策略。

---

### P1-4：LogChannel 存在 shell 拼接注入面
**现象**  
`tail` 与 `grep` 参数由字符串拼接构造命令。

**影响**  
- read-only 工具可能被构造为危险命令。

**建议**  
- 统一参数化执行，禁用 shell 拼接。
- 必要时做 `shlex.quote`，但更推荐 argv 方式。
- 对 `log_path/filter` 做白名单约束（路径前缀、长度、字符集）。

---

### P1-5：LLM 只看到只读工具 schema，却要输出写操作修复计划
**现象**  
诊断阶段工具注册是 read_only；但输出要求包含 write 工具名与参数。

**影响**  
- 计划“看起来合理”，但执行时常失败（工具不存在/参数不匹配）。

**建议**  
- 向模型提供“写工具说明清单（不可调用）+ 参数示例”。
- 修复计划进入执行前做 schema 静态校验（不通过则退回重生成）。

---

### P1-6：模式记忆置信度只增不减
**现象**  
出现失败案例时仍持续提高模式置信度。

**影响**  
- 错误模式会被不断强化，长期拉低诊断准确率。

**建议**  
- 成功 +Δ，失败 -Δ。
- 加入时间衰减与环境标签（机房/机型/版本）分桶。

---

## P2（可并行优化）

1. `infer_topology()` 需要最小可用实现，避免数字孪生大量孤点。  
2. ThinkingTrace 对外推送对象应统一序列化接口（`to_dict` 或 `asdict`）。  
3. Conversational 会话历史需上限与摘要化策略。  
4. 增加诊断并发上限与 token 成本预算。  
5. `kubeconfig` 配置建议单一事实源，避免重复配置漂移。  
6. Canary 成功标准建议改为结构化条件，避免二次引入表达式求值风险。

---

## 3. 修复优先级与排期建议

### 第 1 阶段（1 周）：先保 Demo 可跑 + 安全底线
- 完成 `get_gpu_processes` 工具并修正文档 Demo Trace。
- 移除动态求值路径，落地安全条件解析器。
- 修复 LogChannel 参数化执行，消除注入面。

### 第 2 阶段（1 周）：提升稳定性与可执行性
- async 存储改造（至少关键路径）。
- blast radius 双向遍历 + 拓扑单测。
- 修复计划 schema 校验与失败回退重生成。
- 记忆系统置信度奖惩机制上线。

---

## 4. 建议验收标准

- **安全性**：无动态代码执行路径；工具命令执行无 shell 注入面。  
- **正确性**：3 类标准故障场景中，影响面识别准确率 ≥ 90%。  
- **稳定性**：10 并发诊断会话下，事件循环阻塞告警为 0。  
- **可执行性**：修复计划 schema 一次通过率 ≥ 95%，不合法计划可被拦截。  
- **可回滚性**：所有写操作具备 WAL 且通过回滚演练。

---

## 5. 最终结论

`AIDC-auto-SRE.md` 的架构方向是正确的，具备演进为生产级系统的基础。当前需优先收敛三条主线：
1. **安全实现与安全声明一致**（避免“纸面安全”）；
2. **异步架构名实一致**（避免事件循环被同步 I/O 阻塞）；
3. **LLM 输出可执行、可校验、可回退**（避免“能说不能做”）。

在完成 P0 + 关键 P1 后，再做 Demo 与对外评审会更稳健。
