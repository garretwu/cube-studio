# AIDC Auto SRE Agent 设计评审（重写版）

> 评审目标：在不改变“自由诊断、受控修复”总体架构的前提下，识别当前设计中仍缺失或未落地的关键点，形成可排期整改清单。
>
> 评审结论：整体方向正确，但仍存在若干“生产化落地缺口”，主要集中在**返回契约统一、鉴权落地、并发幂等、HA/容灾、审计可追责**五条主线。

---

## 1. 总体评价

### 1.1 做得好的部分
- 架构边界清晰：诊断（LLM）与修复（确定性执行）分离，原则正确。
- 安全机制框架完整：WAL、审批门控、灰度执行、回滚路径均已纳入。
- 诊断闭环增强明显：引入 Loop Orchestrator 后，多候选根因验证路径更贴近真实生产场景。

### 1.2 当前主要缺口
- 若干核心能力停留在“声明层”，尚未形成“接口契约 + 实现约束 + 验收标准”的闭环。
- API 与修复引擎的错误/返回模型尚未统一，影响前后端和编排器稳定协作。
- 并发控制只覆盖“数量限制”，缺少“资源级互斥 + 告警幂等”机制。

---

## 2. 关键遗失点（按优先级）

## P0（建议优先补齐）

### P0-1：返回契约不一致，存在字段漂移风险
**现象**
- `RemediationResult` 模型定义包含 `steps_total`、`verification_results`、`duration_seconds` 等字段；
- 但多个返回分支未完整填充，且出现了模型未定义字段（如 `reason`）的返回写法。

**影响**
- API 层和前端解析不稳定。
- 编排器难以做统一失败重试与审计归档。

**建议**
- 统一 `DiagnosisSession` / `LoopResult` / `RemediationResult` / API 错误返回契约。
- 建立“成功/失败/回滚/审批拒绝”四类标准响应样例，并补充契约测试。

---

### P0-2：鉴权与 RBAC 仍偏“文档声明”，缺少接口执行点
**现象**
- 文档有角色权限矩阵，但 REST/WebSocket 示例未体现鉴权中间件、角色校验、会话隔离。

**影响**
- 生产部署时容易出现“有权限设计，无权限执行约束”。
- 高风险操作（审批/回滚）缺少强制身份绑定与审计关联。

**建议**
- 在 API/WS 层明确接入：认证（JWT/OIDC）+ 鉴权（RBAC）+ 操作审计（who/when/what）。
- 对 `approve/rollback/handle` 等高风险接口增加最小权限原则和二次确认策略。

---

### P0-3：缺少“同一实体互斥锁 + 告警幂等键”
**现象**
- 当前配置有 `max_concurrent_remediations`，但没有资源级并发互斥（如 node/service 级 lock）；
- 未定义 Alert 去重 fingerprint 与幂等窗口。

**影响**
- 重复告警或并发告警下可能触发重复修复、交叉修复。
- 造成状态抖动、回滚冲突，甚至扩大影响面。

**建议**
- 引入 `resource_lock`（node_id / service_id / incident_scope）。
- 设计 `idempotency_key = alert_fingerprint + time_bucket`，并给出过期与合并策略。

---

## P1（建议本迭代完成）

### P1-1：HA/容灾策略缺失，当前更像单机方案
**现象**
- checkpoint、ontology、memory、wal 均偏本地文件/本地库路径配置；
- 未定义主备切换与恢复流程（RTO/RPO）。

**影响**
- 进程或节点故障后，恢复行为不确定。
- 无法满足生产级可用性要求。

**建议**
- 明确状态分层：强一致状态（WAL/审批）与弱一致状态（trace/缓存）。
- 给出最小 HA 方案：共享存储/外部数据库 + leader 选举 + 恢复演练流程。

---

### P1-2：Secrets 治理策略缺失
**现象**
- 配置中有密码/密钥环境变量引用，但未定义密钥生命周期与管理机制。

**影响**
- 凭据泄漏、权限过大、轮换不及时等风险不可控。

**建议**
- 引入统一密钥管理（Vault/KMS/Secret Manager）。
- 明确轮换周期、最小权限、失效策略、审计记录。

---

### P1-3：审计日志仅 append-only 本地文件，不足以满足高可信追责
**现象**
- 审计记录设计为 `audit.jsonl` append-only，但未定义防篡改链路与异地留存。

**影响**
- 事故复盘时证据可信度不足。
- 合规审计可能不通过。

**建议**
- 本地日志 + 远端不可篡改存储双写（对象存储版本化/WORM）。
- 对高风险动作增加签名链（hash chain）与 trace_id 强绑定。

---

### P1-4：SLO/错误预算未转化为自动化降级策略
**现象**
- 文档有监控指标和统计面板，但缺少“阈值越界后的系统行为定义”。

**影响**
- 当误修复率上升或诊断成功率下降时，系统无法自动收敛风险。

**建议**
- 制定 Agent SLO：诊断成功率、误修复率、平均恢复时长、审批通过后失败率。
- 绑定错误预算策略：超阈值自动降级为 `human_confirm` 或 `read-only` 模式。

---

## P2（可并行优化）

1. 增加“跨组件统一错误码表”，避免不同模块各自定义异常语义。  
2. 增强回滚演练脚本覆盖（审批拒绝、回滚失败、部分成功场景）。  
3. 为 WebSocket 推送增加 backpressure 与断线重连语义定义。  
4. 增加“多租户/多集群隔离”说明（session 与数据访问边界）。  
5. 将 Demo 验证标准扩展为可自动执行的回归集，接入 CI。  

---

## 3. 推荐整改顺序

### 第 1 阶段（1 周）：接口与安全底线
- 统一返回契约与错误模型（P0-1）。
- API/WS 落地鉴权 + RBAC（P0-2）。
- 引入资源级互斥锁与告警幂等（P0-3）。

### 第 2 阶段（1 周）：稳定性与可用性
- HA/容灾方案最小落地（P1-1）。
- Secrets 治理与轮换机制（P1-2）。
- 审计日志防篡改升级（P1-3）。

### 第 3 阶段（0.5 周）：运营化收口
- SLO 与错误预算自动降级策略（P1-4）。
- 故障回归集与自动化验收脚本接入 CI（P2）。

---

## 4. 建议验收标准（重定义）

- **契约一致性**：所有核心接口通过 schema contract test，字段一致率 100%。  
- **权限有效性**：高风险接口未经授权不可执行，越权测试拦截率 100%。  
- **并发安全性**：重复告警与并发告警下，无重复修复与交叉写冲突。  
- **可用性**：单节点故障场景下可恢复，且满足约定 RTO/RPO。  
- **可追责性**：关键操作在审计系统可检索、可验签、可关联 trace_id。  
- **风险可控**：SLO 越界时系统自动降级策略可触发并生效。  

---

## 5. 最终结论

`AIDC-auto-SRE.md` 已具备“可落地”的总体设计框架，但从“能讲清楚”到“能稳定跑在生产”之间，仍需补齐以下最关键闭环：

1. **契约闭环**（统一结果/错误模型）；
2. **权限闭环**（RBAC 在接口执行层强制生效）；
3. **并发闭环**（互斥与幂等保证不重复修复）；
4. **可用性闭环**（HA/容灾可演练、可恢复）；
5. **审计闭环**（日志不可篡改、可追责）。

完成上述 P0/P1 后，再进行对外 Demo 与评审，会更稳健。
