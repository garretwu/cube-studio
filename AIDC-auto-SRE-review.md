# AIDC Auto SRE Agent 设计评审（修订版）

> 评审目标：在不改变“自由诊断、受控修复”总体架构的前提下，识别当前设计中仍缺失或未落地的关键点，形成可排期整改清单。  
> 修订说明：本版根据 `AIDC-auto-SRE-review-feedback.md` 进行校准，重点修正了 P0-1 的表述偏差，并补充了 LLM 降级、告警风暴、Skills 安全边界、Canary 聚合逻辑等关键遗漏。

---

## 1. 总体评价

### 1.1 做得好的部分
- 架构边界清晰：诊断（LLM）与修复（确定性执行）分离，方向正确。
- 安全机制框架完整：WAL、审批门控、灰度执行、回滚路径均已纳入。
- 诊断闭环增强明显：Loop Orchestrator + 多候选根因验证路径，贴近真实生产场景。

### 1.2 当前主要缺口
- 若干能力仍停留在“声明层”，尚未形成“接口契约 + 实现约束 + 验收标准”的闭环。
- 关键问题优先级需重新校准：P0-1 应聚焦“非模型化返回路径”而非笼统字段漂移。
- 生产化遗漏点较多：LLM 降级、告警风暴治理、Skills 扩展安全、数据生命周期等尚未被纳入主评审。

---

## 2. 关键问题清单（按优先级）

## P0（建议优先补齐）

### P0-1：返回契约风险应聚焦“非模型化返回路径”，而非泛化为字段漂移
**现象**
- 设计文档已大量采用 Pydantic v2 模型（如 `RemediationResult`、`DiagnosisResult`、`LoopResult`），模型层一致性已有运行时校验保障。
- 真实风险来自两类路径：
  1) 分支直接返回 `dict`（如 `return {"reason": ...}`）绕过模型；
  2) Optional 字段使用策略不统一（有的缺省为 `None`，有的语义上“应必填”但未强制）。

**影响**
- API/前端/编排器在异常分支下出现解析分叉。
- 审计与重试策略难以基于统一结构自动化执行。

**建议（细化）**
- 强制所有出口统一走 `response_model`，禁止裸 `dict` 返回（包括异常分支）。
- 建立标准错误模型（如 `code/message/details/trace_id/retryable`），替代临时字段。
- 对 Optional 字段做分类：`可缺省` / `必须显式为 null` / `必须赋值`，并补 contract test。

---

### P0-2：鉴权与 RBAC 需落地到 REST/WS 执行点
**现象**
- 有角色权限矩阵，但缺少接口执行层的明确挂载点与失败处理语义。

**影响**
- 容易出现“有权限设计、无权限约束”的上线风险。
- 高风险动作（approve/rollback/handle）难以实现身份强绑定与追责闭环。

**建议（细化）**
- REST：在 FastAPI 路由层统一接 `Depends(get_current_user)` + `Depends(require_role(...))`。
- WebSocket：在 `on_connect` 做 token 校验，并在每条高风险消息执行二次鉴权（防会话劫持/权限漂移）。
- 高风险操作强制记录：`user_id`、`role`、`session_id`、`trace_id`、`request_id`、`decision_reason`。
- 明确 401/403 错误码语义与前端交互策略（重登、降级只读、审批按钮禁用）。

---

### P0-3：并发控制需从“限流”升级为“互斥 + 幂等 + 风暴治理”
**现象**
- 当前仅有 `max_concurrent_remediations`，缺少资源级锁与告警幂等键。
- 尚未覆盖告警风暴/级联故障（上游根因导致下游多点同时告警）的治理策略。

**影响**
- 重复修复、交叉修复与状态抖动风险高。
- 风暴场景下队列积压，可能挤压真正高优先级故障处理。

**建议（细化）**
- 锁模型：`resource_lock(node_id/service_id/incident_scope)`，并明确实现方案（Redis 分布式锁或 etcd lease）。
- 幂等模型：`idempotency_key = alert_fingerprint + time_bucket`，定义 TTL、合并窗口、冲突处理。
- 风暴治理：
  - 引入告警关联聚合（拓扑/依赖关系驱动 root-cause grouping）；
  - 当上游根因处理中时，自动 suppress 下游衍生告警；
  - 队列分级（P0/P1）与背压策略（拒绝低优先级自动修复，仅保留诊断）。

---

## P1（建议本迭代完成）

### P1-1：HA/容灾设计需明确到组件与迁移路径
**现象**
- 当前状态存储偏本地文件/本地库，主备切换与恢复流程未定义。

**影响**
- 进程/节点故障后恢复不确定，难达生产可用性目标。

**建议（细化）**
- 状态分层：
  - 强一致：WAL、审批状态、执行状态（需外部数据库）；
  - 弱一致：trace、缓存、临时推理上下文。
- 明确迁移改造面：OntologyStore、MemoryStore、WAL 三类存储的外置化路径。
- 定义最小 HA 方案：外部 DB + leader 选举 + 故障演练 Runbook（含 RTO/RPO 验收）。

---

### P1-2：LLM 故障降级路径需明确触发、能力边界、恢复机制
**现象**
- 文档声明“主 LLM 不可用可降级规则引擎”，但触发条件/能力边界/恢复机制未定义。

**影响**
- 故障时系统行为不可预测，可能出现半瘫痪或误操作。

**建议**
- 触发条件：超时阈值、连续 N 次 API 错误、错误预算超标。
- 降级能力：明确仅保留阈值告警/规则诊断/禁止自动修复等模式矩阵。
- 恢复策略：健康探针自动回切 + 抖动保护（最小稳定窗口）+ 人工强制切换开关。

---

### P1-3：Skills 扩展框架需补齐安全边界
**现象**
- 已有 allow/deny/ask 与超时机制，但沙箱隔离、输出注入、写权限边界仍不清晰。

**影响**
- 自定义脚本可能扩大执行面，并通过上下文注入影响 LLM 决策。

**建议**
- 隔离：最小权限运行用户 + 文件系统白名单 + 网络访问策略。
- 输出安全：对脚本输出做结构化清洗/截断，禁止原样拼接系统提示词。
- 权限治理：限制 `custom/` 写入主体，纳入变更审计与签名校验。

---

### P1-4：Canary 多条件 success_criteria 需定义聚合语义
**现象**
- `CanaryCondition(field/operator/value)` 已结构化，但多条件判定是 AND 还是 OR 未明确。

**影响**
- 灰度放量决策可能不一致，导致错误升级或保守卡死。

**建议**
- 明确定义默认聚合逻辑（推荐 AND），允许策略级覆写。
- 提供 DSL/配置示例：`all_of`、`any_of`、权重阈值模式。
- 将“条件命中明细”写入审计，支持复盘。

---

### P1-5：Secrets 治理策略需形成闭环
**现象**
- 有密钥环境变量引用，但缺生命周期、轮换与失效策略。

**影响**
- 凭据泄漏、权限过大、轮换滞后等风险不可控。

**建议**
- 引入 Vault/KMS/Secret Manager 统一管理。
- 明确轮换周期、最小权限、吊销流程、审计追踪。

---

### P1-6：审计日志可信度需从“本地 append-only”升级到“可验签可留存”
**现象**
- 当前审计日志偏本地落盘，缺少异地留存和防篡改链路。

**影响**
- 事故复盘与合规审计的证据可信度不足。

**建议**
- 本地 + 远端不可篡改存储（版本化/WORM）双写。
- 高风险事件引入 hash chain 与签名校验，并绑定 trace_id。

---

## P2（可并行优化）

1. 增加跨组件统一错误码表，避免异常语义碎片化。  
2. 引入 OpenTelemetry 端到端 trace（API Gateway → FastAPI → LangGraph → Channels）。  
3. 增强回滚演练脚本覆盖（审批拒绝、回滚失败、部分成功）。  
4. 为 WebSocket 推送增加 backpressure 与断线重连语义。  
5. 增加多租户/多集群隔离说明（session 与数据访问边界）。  
6. 补齐数据生命周期策略：TTL/归档、容量告警、向量索引退化治理。  
7. 将 Demo 验收标准扩展为可自动执行回归集并接入 CI。  

---

## 3. 推荐整改顺序

### 第 1 阶段（1 周）：接口安全与并发底线
- 契约收口：统一响应/错误模型，封堵非模型化返回（P0-1）。
- 权限收口：REST/WS 鉴权与 RBAC 强制落地（P0-2）。
- 稳定性收口：资源锁 + 幂等键 + 告警风暴治理（P0-3）。

### 第 2 阶段（1 周）：可用性与安全增强
- HA 最小落地与恢复演练（P1-1）。
- LLM 降级策略闭环（P1-2）。
- Skills 安全边界加固（P1-3）。
- Canary 多条件聚合规则落地（P1-4）。

### 第 3 阶段（0.5~1 周）：治理与运营化收口
- Secrets 治理与审计防篡改升级（P1-5/P1-6）。
- OTel、数据生命周期、回归集 CI 化（P2）。

---

## 4. 建议验收标准（重定义）

- **契约一致性**：核心接口 contract test 全通过，禁止裸 `dict` 返回。  
- **权限有效性**：高风险接口越权拦截率 100%，WS 重放/劫持场景可阻断。  
- **并发安全性**：重复告警、并发告警、告警风暴下均无重复修复与交叉冲突。  
- **可用性**：单节点故障可恢复，RTO/RPO 达标，LLM 降级可自动触发与恢复。  
- **可追责性**：关键操作可检索、可验签、可关联 trace_id 与 user_id。  
- **长期可运营**：存储容量、向量检索延迟、错误预算越界均有自动告警与治理动作。  

---

## 5. 最终结论

`AIDC-auto-SRE.md` 已具备“可落地”的总体框架，但要达到“可稳定生产运行”，仍需补齐六个闭环：

1. **契约闭环**（模型化返回 + 错误语义统一）；
2. **权限闭环**（RBAC 在 REST/WS 执行层强制生效）；
3. **并发闭环**（资源互斥、幂等、风暴治理）；
4. **可用性闭环**（HA 可演练 + LLM 降级可恢复）；
5. **审计闭环**（防篡改、可验签、可追责）；
6. **运营闭环**（数据生命周期、可观测性、自动化回归）。

完成上述 P0/P1 后再进行对外 Demo 与评审，稳健性会显著提升。
